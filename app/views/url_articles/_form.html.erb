<%= form_for(@url_article) do |f| %>
  <% if @url_article.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@url_article.errors.count, "error") %> prohibited this url_article from being saved:</h2>

      <ul>
      <% @url_article.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


  <div class="field">
    <%= f.label :source %><br>
    <%= f.text_field :source %>
  </div>


  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
